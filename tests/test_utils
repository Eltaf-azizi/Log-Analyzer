
from src.core.utils import parse_timestamp
import pandas as pd



def test_parse_timestamp_apache():
    ts = "12/Dec/2024:19:06:34 +0000"
    dt = parse_timestamp(ts)
    assert isinstance(dt, (pd.Timestamp, type(pd.NaT)))
    assert not pd.isna(dt)

